# QA report, 2026-08-29

**Scope:** Full-site QA sweep (formatting, design, mobile friendliness, readability, content),
every check re-derived from scratch, nothing carried forward from prior reports. This run adds
a first-ever live rendered check of blog.dugong.live in a real browser, desktop and mobile.

**Result: zero defects. No files were modified by this run.**

## What changed since the 08-25 QA

Nothing. No file in the tree has a modification time later than QA-report-2026-08-25.md, and
the working tree is clean: the whole Nº 115 publish plus mesh edits were committed as 54d26fd
("ch", 2026-08-26 14:04 IST) and the branch is level with origin/main, so the deploy is live.
No dispatch shipped on 08-26, 08-27, 08-28, or today; the newest dispatch remains Nº 115
(vacation mode, Priya Singh, 2026-08-25). There is also no blog-run or SEO/GEO report for any
of those dates, consistent with no publish having fired.

## New this run: live rendered verification

Prior QA runs had no browser, so mobile checks were static only. This run loaded the deployed
site in a real browser and verified:

- **Deployed state matches the repo.** blog.dugong.live serves hero ISSUE Nº 115 / AUG 25 · 2026,
  confirming commit 54d26fd is what is live.
- **Zero console errors** on the homepage.
- **Mobile (375x812 emulation): no horizontal overflow anywhere.** Document scroll width equals
  viewport width (375px) on index.html, the Nº 115 post (standard stylesheet), and
  best-shopify-automations-to-set-up.html (superset stylesheet). Zero elements wider than the
  viewport outside the intentionally-overflowing ticker; the playbook `<pre>` fits at 335px with
  overflow-x auto; body text renders at 17px.
- **Desktop (1280px): no overflow, no broken images**, 5 FAQ items rendered on the post checked.
- **Stub redirect works live**: the-vacation-your-store-wont-let-you-take.html lands on the
  Nº 115 canonical URL.
- The hero briefly appears blank in screenshots taken while the CSS "rise" entrance animation
  is mid-flight in a hidden pane; DOM inspection confirms the h1 and all content are present,
  visible, and at opacity 1. Rendering artifact of the capture, not a site defect.

## Structural audit (all recomputed from scratch, all passing)

Inventory: **139 HTML files** = 71 rendered (68 wired posts + index.html + 2 noindexed
rejected drafts) and 68 redirect stubs, plus sitemap.xml, feed.xml, llms.txt, llms-full.txt,
robots.txt, CNAME. Dispatches run Nº 48 through Nº 115, contiguous, no duplicates (Nº 47
present only as the legacy feed guid, as before).

| Check | Result |
|---|---|
| Tag balance / duplicate ids / replacement chars, all rendered pages | 0 problems |
| Exactly one h1; lang="en"; charset; viewport, all 71 rendered pages | 71 / 71 |
| JSON-LD parses on all rendered pages | 0 errors |
| FAQ: 5 questions on all 68 posts; visible == FAQPage JSON, Q and A | 68 / 68, 340 questions |
| Internal links and local assets across all rendered pages | 0 dead |
| Canonical == og:url == filename, wired posts | 68 / 68 |
| Duplicate titles / meta descriptions; descriptions <= 160 chars | 0 / 0 / PASS |
| dateModified == JSON-LD == sitemap lastmod; future dates | 0 mismatches / 0 |
| Card dates AND read-times vs target datePublished/read-time, 3,275 cards site-wide | 0 mismatches |
| Sibling mesh: every unordered pair of 68 wired posts covered; no self-links | PASS (0 uncovered pairs) |
| Index: hero Nº 115 / AUG 25; stat 115; ticker 19 ×2 == 19 Aug pubDates in feed; all 68 posts carded; exactly one card-tall; no ghost links; 69 BlogPosting nodes | PASS |
| Feed item description == index card dek for Nº 115 | identical, 1,651 chars |
| sitemap.xml: valid XML, 69 locs = 68 wired + root, 0 dupes/ghosts, no future lastmod | PASS |
| feed.xml: valid XML, 69 items (68 wired + legacy #dispatch-47), unique guids, weekday-correct pubDates, no future dates | PASS |
| llms.txt: all 68 wired covered, no ghosts, no dupes, llms-full pointer present | PASS |
| llms-full.txt: 142 fence markers balanced, Nº 115 leading newest-first, 0 HTML leftovers, 0 raw em/en dashes, 0 replacement chars | PASS |
| Stubs: 68, bijective onto wired posts (unique targets), all noindexed, all with meta-refresh + JS redirect, all targets live | PASS |
| Rejects (2): noindexed, absent from sitemap/feed/llms/llms-full/index | PASS |
| Author @id resolves to `#priya-singh` on all 68 posts | PASS |
| Styles: standard sheet byte-identical across 64 posts; superset variant on the standing 4; exactly two variants | PASS |
| Mobile statics: viewport everywhere, no `<table>`, no fixed widths over 380px (max-width excluded) — now corroborated by live render | PASS |
| Em/en dashes: 354 raw occurrences classified, all standing chrome (210 CSS comments, 70 ticker spans, 71 head metadata in title/RSS-link/meta, index VOL. 04 ×2 and "— A. Rao" ×1); 0 in body prose | PASS |
| robots.txt AI-crawler allowances; CNAME = blog.dugong.live | PASS |

## Notes for the owner

- **The site is fully published and clean.** Tree committed (54d26fd) and level with
  origin/main; the live site serves Nº 115.
- **No publish has run since 08-25** (four days). If the publish cadence was meant to
  continue, the publish workflow's schedule is worth a look; QA found nothing wrong on the
  site side.
- The publish workflow still writes no run log (standing note since 08-24), and the standing
  recommendation to delete the two rejected drafts on a supervised run remains open.
- Minor, cosmetic only, no action needed: `<title>` tags mix raw em dashes (34 posts) and
  `&mdash;` (the rest, including Nº 115). Both render identically; flagged only for
  template consistency if the generator is ever touched.
- `.fuse_hidden*` files and the untracked `.claude/` folder are tooling/mount artifacts,
  ignored per convention.

## Verification method note

All counts recomputed from the files; nothing carried forward. Card-date comparison uses the
site's unpadded-day convention (AUG 9, not AUG 09). Live checks ran against blog.dugong.live
in a real browser with mobile (375x812) and desktop (1280px) viewports, measuring DOM scroll
widths, element bounding boxes, image load state, console errors, and stub redirect behavior.
This run made no edits; the site needed none.
