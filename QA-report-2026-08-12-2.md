# QA report, 2026-08-12 (second run)

**Scope:** Full-site QA sweep (formatting, design, mobile friendliness, readability, content),
every check re-derived from scratch. Baseline: this morning's QA covered through dispatch
Nº 104; since then the two 08-12 blog runs shipped **Nº 105** ("The package that stops moving" /
`how-to-catch-shopify-orders-stuck-in-transit.html`) and **Nº 106** ("The misprice that sells
all night" / `how-to-catch-pricing-errors-on-shopify-before-orders-flood-in.html`), both by
Priya Singh, both 2026-08-12, plus mesh/index/sitemap/feed/llms rewiring. Inventory confirmed:
**121 HTML files** — 62 rendered (59 wired posts + index.html + 2 noindexed rejected drafts)
and 59 redirect stubs, plus sitemap.xml, feed.xml, llms.txt, robots.txt, CNAME, favicons.
Wired dispatches run Nº 48 through Nº 106 with no gaps and no duplicates.

**Result: 1 defect, found and fixed.** A narrative day-count slip in Nº 105's closing replay
contradicted the article's own USPS rule. Everything else — including all of Nº 106 — passed
on first inspection. One file changed.

## The defect, and the fix

**Nº 105, "Run order 2213 again" replay.** The package mails on a Wednesday, and the article
states three times (body, NOTE box, FAQ) that USPS accepts a Missing Mail search once **7 days
have passed since the mailing date** — which lands on the following **Wednesday**. The replay
had the trace going in "the hour it becomes eligible" on **Tuesday**, i.e. day 6, a day before
the article's own eligibility rule. Fixed by moving the promised update and the trace filing
from Tuesday to Wednesday (two words, one sentence pair). The downstream beats are unchanged
and still consistent: Thursday the box surfaces, Saturday it lands at day 10, the claim window
closes untouched a month and a half later (60-day order, correct). Re-verified after the edit:
tag balance clean, JSON-LD parses, FAQ match holds, `<style>` still byte-identical (md5) to
the standard sheet. dateModified 2026-08-12 already covers an 08-12 edit; no sitemap/feed
ripple needed.

## Nº 105 deep review (new since last QA)

- **External fact spot-checks (re-verified against primary sources this run).** The
  FulfillmentEventStatus enum on shopify.dev (2026-07, latest) lists exactly **eleven** values
  — ATTEMPTED_DELIVERY, CARRIER_PICKED_UP, CONFIRMED, **DELAYED**, DELIVERED, FAILURE,
  IN_TRANSIT, LABEL_PRINTED, LABEL_PURCHASED, OUT_FOR_DELIVERY, READY_FOR_PICKUP — matching
  the article's "eleven fulfillment event statuses" and its load-bearing point that a
  `delayed` slot exists which nothing in the admin acts on. Descartes' 2024 study surveyed
  **8,000** consumers across North America and Europe: **67%** hit a delivery problem in the
  three-month window and **63%** of those took action against the brand — the article's pairing
  is the 2024 study's, correct. USPS Missing Mail at 7 days from mailing and UPS claims opening
  24 hours after the scheduled delivery date match the carriers' published rules.
- **Internal consistency.** Opening timeline: Wednesday-evening scan → "five days old" Monday
  → "day six" Wednesday is consistent under elapsed-full-days counting (see non-defects).
  "Day 12 has spent a fifth of the claim window" = 12/60, correct. First-mile threshold appears
  as "24 hours" (playbook), "after a day" (body), "24 to 48 hours" (FAQ) — overlapping ranges,
  tight rule in the playbook, not contradictory. Ticker `Nº 105 — AUG 12, 2026` matches
  datePublished/dateModified 2026-08-12. wordCount 2600 within the standing tolerance.
  "10 MIN READ" consistent across ticker, index card, and mesh cards site-wide.
- **FAQ.** Three visible `.faq-q` questions match FAQPage JSON-LD exactly (parsed, not
  regexed); answers agree with the body on every threshold and carrier window.
- **Design/mobile.** `<style>` byte-identical (md5) to the standard mobile-validated sheet.
  Viewport present; no hard widths over 380px; no tables; pre uses pre-wrap.
- **Wiring.** All 57 older posts + Nº 106 link to it; its READ NEXT grid leads with Nº 106
  then curated older cards, no duplicates. Stub `the-package-that-stops-moving.html` noindexed
  and refreshing to the live file. Feed item 2 (pubDate Wed, 12 Aug 2026 — weekday correct),
  sitemap loc + lastmod 2026-08-12, llms.txt entry second, byline uniform.

## Nº 106 deep review (new since last QA)

- **External fact spot-checks (re-verified against coverage this run).** 6pm.com, May 2010:
  prices capped at **$49.95** from **midnight to 6 am Pacific**, cost **over $1.6M**, honored in
  full — matches Digital Commerce 360, Fast Company, and Techdirt coverage, and the article's
  "window ran six hours" arithmetic is right. RepricerExpress, **Dec 12 2014, 7–8 pm**: listings
  from **~2,000 Amazon UK sellers** at 1p for about an hour; the costume seller (Kiddymania)
  reported **675 costumes** gone and a **£15,000–£20,000** loss; the toy seller took **1,600
  orders** inside the hour; Amazon's line that it "cannot cancel or reimburse orders that have
  already shipped" is nearly verbatim. Flow's product trigger list (eight triggers, inventory
  in, price out) matches the current documented set, and the products/update webhook's missing
  changed-field/previous-value is correct.
- **Internal consistency.** $249 → $2.49 is the stated 99% drop; honoring two orders costs
  2 × $246.51 = **$493** against list, as the alert says; save at 9:41 → price restored 9:51;
  71 orders / 64 on one product / "the 71 orders it replaced" callback all agree; the €16,99 →
  €1699,00 separator example is exactly ×100. Ticker `Nº 106 — AUG 12, 2026` matches
  datePublished/dateModified. wordCount 2700 vs 2810 measured — within tolerance. "10 MIN READ"
  consistent everywhere.
- **FAQ.** Three questions match FAQPage JSON-LD exactly. The FAQ's "five doors" enumerates
  admin edits, bulk editor, CSV import, apps with write scope, and the sale-setup compare-at
  swap; the body's "count the ways" paragraph names the first four and grounds the fifth in
  the audit section (price above its own compare-at). Ruled consistent, not a count error.
- **Design/mobile.** `<style>` byte-identical (md5) to the standard sheet; viewport; no hard
  widths over 380px; no tables.
- **Wiring.** Newest post: index hero chip ISSUE Nº 106 + AUG 12 date, the single card-tall,
  first card in every older post's READ NEXT grid (all 58), first article entry in llms.txt,
  feed item 1, sitemap lastmod 2026-08-12. Stub `the-misprice-that-sells-all-night.html`
  noindexed and refreshing to the live file.

## Structural audit (all passing after the fix)

| Check | Result |
|---|---|
| Tag balance / stray tags / duplicate ids, all 121 files | 0 problems |
| Exactly one h1 per rendered page; lang="en"; charset; viewport | 62 / 62 |
| JSON-LD parses on all rendered pages (incl. index @graph) | 0 errors |
| FAQ JSON-LD matches visible `.faq-q` text (parsed, not regexed) | PASS, all posts |
| Internal links + in-page/cross-page anchors resolve (root-relative included); favicon.svg/png + apple-touch-icon + og-image exist | 0 dead |
| Canonical == og:url == filename; og/twitter sets complete | 60 / 60 |
| Duplicate titles / duplicate meta descriptions | 0 / 0 |
| Meta descriptions present, 160 chars or under; titles carry site suffix | 60 / 60 |
| Dispatch numbering Nº 48–106 (all glyph forms) | contiguous, 0 dupes |
| Ticker Nº/date vs datePublished | 59 / 59 consistent |
| Future dates (datePublished, dateModified, lastmod, pubDate) | 0 |
| Feed pubDate/lastBuildDate weekday agrees with date | 61 / 61 |
| feed.xml well-formed XML; no duplicate guids; newest-first | PASS |
| Mojibake / replacement chars / placeholder text (TODO, TKTK, lorem) | 0 |
| en-US typo sweep (word-boundary, 60-term list) | 0 hits |
| Repeated-word scan | 15 hits, all title/dek or topic-cloud adjacency false positives |
| sitemap.xml: 60 locs = 59 wired + index, no dupes/ghosts/missing | PASS |
| feed.xml: 60 items = 59 wired + pinned Issue 47 essay (guid isPermaLink known) | PASS |
| llms.txt: 59 article entries, all wired, newest-first (Nº 106 → Nº 48), uniform bylines | PASS |
| Stubs: 59, bijective onto the 59 wired posts, each noindex + live target | PASS |
| Sibling mesh presence: older links every newer, all 1,711 pairs | 0 missing |
| Mesh grid order: newer-than-self block newest-first and contiguous from Nº 106, then curated older; no dup cards | 0 violations, all 59 grids |
| Index: 59 unique post cards newest-first; exactly one card-tall (Nº 106); hero ISSUE Nº 106; "10 NEW DISPATCHES THIS MONTH" = 10 actual Aug posts | PASS |
| Index card read-times (`.post-read`) agree with post tickers | 0 mismatches |
| Mobile: viewport everywhere; shared standard sheet on 55 posts + superset on lead essay/3 embed posts (two variants exactly, intentional); `pre` uses pre-wrap; no hard widths > 380px; no tables | PASS |
| robots.txt sitemap line; CNAME = blog.dugong.live | PASS |
| Rejects (2): still noindexed, linked from nowhere, absent from sitemap/feed/llms | PASS |

## Known non-defects, deliberately not changed

- **Nº 105 "day six."** Wednesday-evening scan → she writes the following Wednesday, "day
  six": consistent under elapsed-complete-days counting, the same convention that makes
  Monday "five days old" (Wed evening → Mon evening = 5.0 days). Left as written.
- **Nº 105 UPS window phrasing.** The body/NOTE anchor the ~60-day claim notice to the ship
  date, hedged with "roughly"; UPS's own anchor is the scheduled delivery date. Measuring from
  the ship date shortens the deadline, i.e. errs conservative for the merchant. Left as
  written.
- **Nº 105 "picked_up."** Prose shorthand for the enum's CARRIER_PICKED_UP in a
  four-example aside; the load-bearing claims (eleven statuses, `delayed` present) are exact.
- The two style variants among rendered posts (standard sheet on 55 incl. Nº 105/106;
  superset with hero + signup CSS on the lead essay + 3 signup-embed posts) remain intentional.
- The style-less rejected draft, the "is is" construction, wordCount tolerances, and the
  pinned-essay feed item conventions carry over unchanged from prior rulings.

## Verification method note

No headless browser exists in this sandbox, so mobile checks remain static: CSS-level
verification plus byte-identity of every standard post's stylesheet (incl. Nº 105 and Nº 106)
with the set validated on-device in `f848267`. External fact checks this run used live fetches
of shopify.dev (FulfillmentEventStatus enum, FulfillmentEvent resource) and web searches over
primary coverage (Descartes 2024 press release and trade coverage; Digital Commerce 360 / Fast
Company / Techdirt for 6pm.com; ChannelX / Fortune / Register coverage for RepricerExpress).

## Notes for the owner

- Working tree left uncommitted, per scheduled-run convention: the two 08-12 runs' files
  (Nº 105, Nº 106, mesh/index/sitemap/feed/llms updates), this run's one-sentence fix to
  Nº 105, and this report await your commit. Nothing on blog.dugong.live changes until
  pushed. Suggested message: "dispatches 105-106 + QA".
- The `.fuse_hidden*` files at the folder root and the empty `Dugong_Blogs/` directory are
  filesystem artifacts, not site content; safe to ignore, tidy on a supervised run if they
  bother you.
- The two noindexed rejected drafts remain untouched; delete on a supervised run when ready.
