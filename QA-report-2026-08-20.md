# QA report, 2026-08-20

**Scope:** Full-site QA sweep (formatting, design, mobile friendliness, readability, content),
every check re-derived from scratch. No publish run or SEO/GEO pass has landed since the
2026-08-16 reports; the site still stands at **Dispatch Nº 110** and no run report dated after
08-16 exists in the folder.

Inventory re-confirmed: **129 HTML files** = 66 rendered (63 wired posts + index.html + 2
noindexed rejected drafts) and 63 redirect stubs, plus sitemap.xml, feed.xml, llms.txt,
robots.txt, CNAME, favicons and og-image.

**Result: 3 defects found, all fixed by this run. All in
`how-to-automatically-capture-payments-on-shopify.html`, all the same species: stale
card-date chrome on three sibling cards.**

## Defects found and fixed

The related-grid cards in the capture-payments post carried publish dates that predate the
targets' actual datePublished, evidently the originally planned dates before those three
dispatches were rescheduled. Every other post carding the same targets (35 to 37 cards each)
already shows the correct date; this file was the lone outlier on all three.

1. Card for `how-to-reconcile-shopify-payouts-with-your-sales.html`: `JUN 13` fixed to `JUN 29`.
2. Card for `how-to-stop-card-testing-attacks-on-shopify.html`: `JUN 10` fixed to `JUL 1`.
3. Card for `how-to-set-up-pre-orders-on-shopify-without-holding-in-stock-items.html`:
   `JUN 20` fixed to `JUN 30`.

Card read-times on the same three cards were checked and were already correct, so only the
date strings moved. The card-date-vs-datePublished comparison (2,834 cards site-wide) is new
to this pass; prior passes compared read-times only, which is how these three survived since
publication.

## Structural audit (all recomputed from scratch, all passing after fixes)

| Check | Result |
|---|---|
| Tag balance / duplicate ids / replacement chars / mojibake, all 129 files | 0 problems |
| Exactly one h1; lang="en"; charset; viewport, all 66 rendered pages | 66 / 66 |
| JSON-LD parses, duplicate-key scan at object-pairs level, all rendered pages | 0 errors |
| FAQ JSON-LD questions match visible `.faq-q` text (entity-decoded), counts == 5 | 63 / 63 |
| Internal links, in-page anchors, cross-page anchors, local assets | 0 dead |
| Canonical == og:url == filename (index canonical = site root) | 64 / 64 |
| Duplicate titles / meta descriptions; descriptions <= 160 chars | 0 / 0 / PASS |
| dateModified == article:modified_time; future dates anywhere | 0 mismatches / 0 |
| Card read-times vs target tickers, 2,834 cards site-wide | 0 mismatches |
| Card dates vs target datePublished, 2,834 cards site-wide (new check) | 0 after fixes |
| Sibling mesh: every unordered pair of the 63 wired posts covered; no self-links | PASS |
| Index: hero Nº 110 / AUG 16 / ticker 14 ×2 == 14 Aug pubDates in feed; all 63 posts carded; no links to non-wired pages | PASS |
| sitemap.xml: 64 locs = 63 wired + root, 0 dupes/ghosts, no future lastmod | PASS |
| feed.xml: unique guids, weekday-correct pubDates, no future dates; the `#dispatch-47` guid is isPermaLink="false" and matches the index JSON-LD @id, not a ghost | PASS |
| llms.txt: all 63 wired covered, no ghosts | PASS |
| Stubs: 63, bijective onto wired posts, all noindexed, all targets live | PASS |
| Rejects (2): noindexed, unlinked, absent from sitemap/feed/llms | PASS |
| Styles: standard sheet byte-identical across 59 posts; superset variant on the standing 4; exactly two variants | PASS |
| Mobile statics: viewport everywhere, no `<table>`, no fixed CSS widths over 380px | PASS |
| Em/en dashes and curly double quotes in authored article copy | 0 hits |
| Typo and placeholder sweep (three "placeholder" hits are legitimate prose, non-defects) | 0 defects |
| robots.txt sitemap line; CNAME = blog.dugong.live | PASS |

## Notes for the owner

- **index.html carries an mtime of today, 2026-08-20 23:25 IST**, minutes before this QA
  started, with no publish-run or SEO report dated after 08-16. Its content verifies clean on
  every index check (hero Nº 110, AUG 16, stat 110, ticker 14 ×2, full card coverage, JSON-LD
  incl. the duplicate-key scan), so whatever wrote it was content-consistent, but an
  unattributed write to the homepage is worth knowing about. Nothing to repair.
- The card-date check that caught today's three defects has been added to the audit alongside
  the read-time check, so this class cannot pass silently again.
- Standing recommendations remain: add the index hero strings to the publish checklist,
  stagger the three schedules by at least an hour, and delete the two rejected drafts on a
  supervised run.
- The working tree remains uncommitted since the last commit (Aug 14); it now also holds this
  run's three-date repair and this report. Nothing on blog.dugong.live changes until pushed.
- `.fuse_hidden*` files at the folder root are mount artifacts, ignored per convention.

## Verification method note

No headless browser in this sandbox, so mobile checks are static (viewport, width caps, wrap
rules) plus byte-identity of the standard stylesheet across the 59-post set. All counts above
were recomputed from the files, not carried forward from prior reports.
